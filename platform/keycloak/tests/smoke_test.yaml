suite: chart smoke coverage
templates:
  - charts/keycloak/templates/serviceaccount.yaml
tests:
  - it: renders charts/keycloak/templates/serviceaccount.yaml
    asserts:
      - hasDocuments:
          count: 1

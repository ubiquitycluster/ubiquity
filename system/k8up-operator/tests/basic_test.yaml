suite: test k8up-operator wrapper rendering
templates:
  - "*.yaml"
tests:
  - it: should remain an empty Helm wrapper because kustomize owns K8up resources
    asserts:
      - hasDocuments:
          count: 0

# Backup and Restore

## Overview
The `ubiquity backup` and `ubiquity restore` commands manage cluster state backups.

## Usage
​```
ubiquity backup
ubiquity restore /path/to/backup
​```

## What's included
- Provisioning state (~/.ubiquity/state.json)
- Configuration files (.env, .ubiquity.yaml)